# Python 3.13（开发机）/ 3.13（服务器）
# 安装：D:\mywebsite\.venv\Scripts\python.exe -m pip install -r backend\requirements.txt
Django==5.2.17
django-simpleui==2026.1.13
djangorestframework==3.18.1
# —— GitHub 集成新增 ——
django-cors-headers==4.9.0
django-allauth==65.19.4
requests==2.34.2
asgiref==3.12.1
sqlparse==0.6.0
tzdata==2026.4
certifi==2026.7.22
charset-normalizer==3.5.1
idna==3.20
urllib3==2.8.0
# —— 云部署（Serverless / PaaS）新增 ——
# 数据库：用 DATABASE_URL 连托管 Postgres（本地仍回落到 SQLite）
dj-database-url==3.1.2
# Postgres 驱动（Django 连 Postgres 必需，缺了会在部署启动时直接报错）
psycopg[binary]==3.3.6
# 对象存储：Cloudflare R2 / S3 兼容后端（PaaS 本地磁盘是临时的）
django-storages==1.14.6
boto3==1.43.102
botocore==1.43.102
s3transfer==0.19.2
jmespath==1.1.0
python-dateutil==2.9.0.post0
six==1.17.0
# 静态文件：生产环境由 WhiteNoise 托管（后台/SimpleUI 样式）
whitenoise==6.12.0
# 头像用 ImageField，Django 需要 Pillow 才能校验图片
Pillow==11.3.0
